services:
  service_pg_db:
    image: postgres:16
    container_name: pg_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: '12345'
      POSTGRES_DB: test
    ports:
      - "9432:5432"
    volumes:
      - ./pg_data:/var/lib/postgresql/data
